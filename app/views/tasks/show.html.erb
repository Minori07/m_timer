<header>
  <div class="temp_menu">
    <%= link_to image_tag('plus.png'), new_task_path %>
    <h1><%= @task.title %></h1>
    <%= image_tag "dust.png" %>
  </div>
</header>
<% @task.todo.each do |t| %>
<table>
    <td><%= t.t %></td>
  </tr>
  <tr>
  	<th>Time</th>
    <td><%= t.at %></td>
  </tr>
<!--   <tr>
    <th>User.Email</th>
    <td><%= @task.user.email %></td>
  </tr> -->
</table>
<% end %>
<p><%= link_to '一覧に戻る', tasks_url %></p>