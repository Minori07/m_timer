<header>
  <div class="temp_menu">
    <%= link_to image_tag('plus.png'), new_task_path %>
    <h1>テンプレート一覧</h1>
    <%= image_tag "dust.png" %>
  </div>
</header>
<!-- <table> -->
  <% @tasks.each do |task| %>
  <%= link_to(task_path(task), class:'list-task') do%>
    <div class="task">
      <table>
    <!-- <%= link_to image_tag('block.png'), edit_task_path(task) %> -->
  <tr>
    <td><%= link_to image_tag('delete.png'), task_path(task), method: :delete %>
    <!-- <td><%= task.id %></td> -->
    <td><%= task.time %></td>
    <!-- <td><%= task.week %></td> -->
    <td><%= task.title %></td>
<!--     <td><%= link_to 'Show', task_path(task) %></td> -->
    </td>
  </tr>
</table>
  </div>
  <% end %>
  <% end %>
<!-- </table> -->